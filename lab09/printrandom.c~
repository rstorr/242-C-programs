#include <stdlib.h>

int main (int argc, char *argv[]) {

    srand(time(NULL));
    
    while (atoi(argv[1]) > i){
        int r = rand();
        printf("%d\n", r);
    }
    
    return EXIT_SUCCESS;
}
